[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
  "pnpm --filter @workspace/ip-logger run build",
  "pnpm --filter @workspace/api-server run build"
]

[start]
cmd = "node artifacts/api-server/dist/index.mjs"
